#include <string.h>

/**
 * _strcmp - compares two strings
 *
 * @s1: string No 1
 * 
 * @s2: string No 2
 *
 * Return: an integer less than, equal to, or greater
 * than zero if s1 is found, respectively,
 * to beless than, to match, or be greater than s2
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
